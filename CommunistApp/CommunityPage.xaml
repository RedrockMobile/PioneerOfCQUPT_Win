<Page
    x:Class="CommunistApp.CommunityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CommunistApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions >
            <RowDefinition Height="35"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Crimson" BorderThickness="0" Margin="0,0,0,-50">
            <TextBlock Text="学习心得分享" Foreground="Azure"
                       FontFamily="Microsoft YaHei UI" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25"/>
        </Grid>
        <Grid Grid.Row="1">
            <ListView >
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Grid Margin="10">
                            <TextBlock Name="name"/>
                            <TextBlock Name="date"/>
                            <TextBlock Name="content"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
